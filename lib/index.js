const _data = require("./data");
const _users = require("./users");
const _tokens = require("./tokens");
const _checks = require("./serviceChecks");
module.exports = {
    _data,
    _users,
    _tokens,
    _checks
}